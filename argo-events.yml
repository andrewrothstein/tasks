---
version: '3'
includes:
  argo-raw:
    taskfile: argo-raw.yml
    internal: true
    vars:
      n: argo-events
      v: 1.7.6
      p: events
tasks:
  default:
    deps:
      - apply
  apply:
    cmds:
      - task: argo-raw:apply
  delete:
    cmds:
      - task: argo-raw:delete
