---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    vars:
      n: tailscale
      url: https://pkgs.tailscale.com/helmcharts
tasks:
  default:
    deps:
      - task: helm-repo:add
