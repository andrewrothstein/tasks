---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: k8ssandra
      url: https://helm.k8ssandra.io/stable
tasks:
  default:
    deps:
      - task: add
