---
version: '3'
includes:
  repo: kiali-repo.yml
  helm: helm.yml
tasks:
  default:
    deps:
      - upgrade
  upgrade:
    vars:
      chart_repo_name: '{{ .chart_repo_name | default "kiali" }}'
      chart: '{{ .chart | default "kiali-operator" }}'
      chart_ver: '{{ .chart_ver | default "v1.87.0" }}'
      namespace: '{{ .namespace | default "kiali-operator" }}'
      install: '{{ .install | default "true" }}'
      create_namespace: '{{ .create_namespace | default "true" }}'
      name: '{{ .name | default "kiali" }}'
    deps:
      - task: repo
    cmds:
      - task: helm:upgrade
        vars:
          install: '{{ .install }}'
          chart_repo_name: '{{ .chart_repo_name }}'
          chart: '{{ .chart }}'
          chart_ver: '{{ .chart_ver }}'
          namespace: '{{ .namespace }}'
          create_namespace: '{{ .create_namespace }}'
          name: '{{ .name }}'
          helm_args: -f kiali-values.yml
  delete:
    vars:
      namespace: '{{ .namespace | default "kiali-operator" }}'
      name: '{{ .name | default "kiali" }}'
    cmds:
      - task: helm:delete
        vars:
          namespace: '{{ .namespace }}'
          name: '{{ .name }}'
