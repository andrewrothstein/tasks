---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: litmuschaos
      url: https://litmuschaos.github.io/litmus-helm/
tasks:
  default:
    deps:
      - task: add
