---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    vars:
      n: jaegertracing
      url: https://jaegertracing.github.io/helm-charts
tasks:
  default:
    deps:
      - task: helm-repo:add
