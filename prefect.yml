---
version: '3'
vars:
  PREFECT_VER: 2026.2.12200748
includes:
  repo: prefect-repo.yml
  kk:
    taskfile: kubectl.yml
    vars:
      n: prefect
      labels: 'istio.io/dataplane-mode=ambient'
  server:
    taskfile: helm.yml
    vars:
      namespace: prefect
      name: prefect-server
      chart_repo_name: prefect
      chart_ver: '{{ .PREFECT_VER }}'
      chart: prefect-server
      helm_args: -f prefect-server-values.yml
  worker:
    taskfile: helm.yml
    internal: true
    vars:
      namespace: prefect
      name: prefect-worker
      chart_repo_name: prefect
      chart_ver: '{{ .PREFECT_VER }}'
      chart: prefect-worker
      helm_args: -f prefect-worker-values.yml
  ingress:
    taskfile: tailscale-ingress.yml
    vars:
      namespace: prefect
      name: tailscale-ingress
      helm_args: -f prefect-tailscale-ingress-values.yml
tasks:
  default:
    deps:
      - upgrade
  upgrade:
    deps:
       - task: repo
    cmds:
      - task: kk:ns-create
      - task: server:upgrade
      - task: worker:upgrade
      - task: ingress:upgrade
  delete:
    cmds:
      - task: ingress:delete
      - task: worker:delete
      - task: server:delete
      - task: kk:ns-delete
  check:
    deps:
      - task: repo
    cmds:
      - task: server:check
      - task: worker:check
