---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: fleet
      url: https://rancher.github.io/fleet-helm-charts/
tasks:
  default:
    deps:
      - task: add
