---
version: '3'
includes:
  repo: flux-repo.yml
  helm: helm.yml
tasks:
  default:
    deps:
      - upgrade
  upgrade:
    vars:
      chart_repo_name: '{{ .chart_repo_name | default "fluxcd-community" }}'
      chart: '{{ .chart | default "flux2" }}'
      chart_ver: '{{ .chart_ver | default "2.13.0" }}'
      namespace: '{{ .namespace | default "flux-system" }}'
      install: '{{ .install | default "true" }}'
      create_namespace: '{{ .create_namespace | default "true" }}'
      name: '{{ .name | default "flux" }}'
    deps:
      - task: repo
    cmds:
      - task: helm:upgrade
        vars:
          install: '{{ .install }}'
          chart_repo_name: '{{ .chart_repo_name }}'
          chart: '{{ .chart }}'
          chart_ver: '{{ .chart_ver }}'
          namespace: '{{ .namespace }}'
          create_namespace: '{{ .create_namespace }}'
          name: '{{ .name }}'
#          helm_args: -f flux-values.yml
  delete:
    vars:
      namespace: '{{ .namespace | default "flux" }}'
      name: '{{ .name | default "flux" }}'
    cmds:
      - task: helm:delete
        vars:
          namespace: '{{ .namespace }}'
          name: '{{ .name }}'
