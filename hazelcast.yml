---
version: '3'
includes:
  hazelcast-repo: hazelcast-repo.yml
  kubectl:
    taskfile: kubectl.yml
    internal: true
    vars:
      n: hazelcast
  platform-operator:
    taskfile: helm.yml
    internal: true
    vars:
      namespace: hazelcast
      name: operator
      chart_repo_name: hazelcast
      chart_ver: 5.11.0
      chart: hazelcast-platform-operator
      install: 'true'
      helm_args: -f hazelcast-platform-operator-values.yml
tasks:
  default:
    deps:
      - upgrade
  upgrade:
    deps:
       - task: hazelcast-repo
    cmds:
      - task: kubectl:ns-create
      - task: platform-operator:upgrade
  delete:
    cmds:
      - task: platform-operator:delete
      - task: kubectl:ns-delete
