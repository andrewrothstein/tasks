---
version: '3'
includes:
  kk:
    taskfile: kubectl.yml
    vars:
      n: crossplane-aws
      k: crossplane-aws
tasks:
  default:
    deps:
      - task: kapply
  upgrade:
    cmds:
      - task: kk:ns-create
      - task: kk:kapply
  delete:
    cmds:
      - task: kk:kdelete
