---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    internal: true
    vars:
      n: yugabytedb
      url: https://charts.yugabyte.com
tasks:
  default:
    deps:
      - task: helm-repo:add
