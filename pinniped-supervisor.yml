---
version: '3'
includes:
  pinniped:
    taskfile: pinniped.yml
    vars:
      APP: pinniped-supervisor
tasks:
  default:
    cmds:
      - task: deploy
  deploy:
    cmds:
      - task: pinniped:deploy
  delete:
    cmds:
      - task: pinniped:delete
