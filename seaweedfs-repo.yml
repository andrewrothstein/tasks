---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    vars:
      n: seaweedfs
      url: https://seaweedfs.github.io/seaweedfs/helm
tasks:
  default:
    deps:
      - task: helm-repo:add
