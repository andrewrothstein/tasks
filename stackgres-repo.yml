---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    internal: true
    vars:
      n: stackgres-charts
      url: https://stackgres.io/downloads/stackgres-k8s/stackgres/helm/
tasks:
  default:
    deps:
      - task: helm-repo:add
