---
version: '3'
includes:
  argo-release:
    taskfile: argo-release.yml
    internal: true
    vars:
      chart: argo-cd
      chart_ver: 7.8.27
      name: argo-cd
      namespace: argocd
      labels: 'istio.io/dataplane-mode=ambient'
tasks:
  default:
    deps:
      - upgrade
  test-secret:
    vars:
      adminSecret: '{{ .adminSecret | default "${ARGOCD_ADMIN_PASSWORD}" }}'
    cmds:
      - test {{ .adminSecret | quote }}
  upgrade:
    vars:
      adminSecret: '{{ .adminSecret | default "${ARGOCD_ADMIN_PASSWORD}" }}'
    cmds:
      - task: test-secret
        vars:
          adminSecret: '{{ .adminSecret }}'
      - task: argo-release:upgrade
        vars:
          helm_args: |-
            -f argocd-values.yml \
              --set configs.secret.argocdServerAdminPassword=$(htpasswd -nbBC 10 "" {{ .adminSecret | quote }} | tr -d ':\n' | sed 's/$2y/$2a/')
  delete:
    cmds:
      - task: argo-release:delete
  check:
    deps:
      - task: argo-release:check
  login:
    vars:
      server: '{{ .server | default "argocd.elephant-frog.ts.net" }}'
      grpc_web: '{{ .grpc_web | default "true" }}'
      username: '{{ .username | default "admin" }}'
      password: '{{ .password | default "${ARGOCD_ADMIN_PASSWORD}" }}'
    cmds:
      - |-
        argocd login \
          {{ .server | quote }}
          {{- if eq "true" .grpc_web }} \
          --grpc-web
          {{- end }}
          {{- if .username }} \
          --username {{ .username | quote }}
          {{- end }}
          {{- if .password }} \
          --password {{ .password | quote }}
          {{- end }}
