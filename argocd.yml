---
version: '3'
includes:
  argo-raw:
    taskfile: argo-raw.yml
    internal: true
    vars:
      n: argo-cd
      p: cd
      v: 2.7.10
tasks:
  default:
    deps:
      - apply
  apply:
    cmds:
      - task: argo-raw:apply
  delete:
    cmds:
      - task: argo-raw:delete
