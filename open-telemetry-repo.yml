---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: open-telemetry
      url: https://open-telemetry.github.io/opentelemetry-helm-charts
tasks:
  default:
    deps:
      - task: add
