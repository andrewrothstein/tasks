---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    internal: true
    vars:
      n: k8sgpt
      url: https://charts.k8sgpt.ai
tasks:
  default:
    deps:
      - task: helm-repo:add
