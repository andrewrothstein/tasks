---
version: '3'
includes:
  helm-repo: helm-repo.yml
  helm: helm.yml
tasks:
  default:
    deps:
      - upgrade
  upgrade:
    vars:
      chart_repo_name: '{{ .chart_repo_name | default "artifact-hub" }}'
      chart: '{{ .chart | default "artifact-hub" }}'
      chart_ver: '{{ .chart_ver | default "1.18.0" }}'
      namespace: '{{ .namespace | default "artifact-hub" }}'
      install: '{{ .install | default "true" }}'
      create_namespace: '{{ .create_namespace | default "true" }}'
      name: '{{ .name | default "artifact-hub" }}'
    deps:
      - task: helm-repo:add
        vars:
          n: '{{ .chart_repo_name }}'
          url: https://artifact-hub.github.io/helm-charts
    cmds:
      - task: helm:upgrade
        vars:
          install: '{{ .install }}'
          chart_repo_name: '{{ .chart_repo_name }}'
          chart: '{{ .chart }}'
          chart_ver: '{{ .chart_ver }}'
          namespace: '{{ .namespace }}'
          create_namespace: '{{ .create_namespace }}'
          name: '{{ .name }}'
#          helm_args: -f artifact-hub-values.yml
  delete:
    vars:
      namespace: '{{ .namespace | default "artifact-hub" }}'
      name: '{{ .name | default "artifact-hub" }}'
    cmds:
      - task: helm:delete
        vars:
          namespace: '{{ .namespace }}'
          name: '{{ .name }}'
