---
version: '3'
includes:
#  metallb: metallb.yml
  metallb: pet.yml

  tailscale: tailscale.yml
#  tailscale: pet.yml

#  ingress: nginx-ingress.yml
  ingress: pet.yml


tasks:
  default:
    deps:
      - up
  up:
    cmds:
      - task: metallb:upgrade
      - task: tailscale:upgrade
      - task: ingress:upgrade
  delete:
    cmds:
      - task: ingress:delete
      - task: tailscale:delete
      - task: metallb:delete
