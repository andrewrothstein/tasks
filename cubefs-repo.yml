---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: cubefs
      url: https://cubefs.github.io/cubefs-helm
tasks:
  default:
    deps:
      - task: add
