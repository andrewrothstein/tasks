---
version: '3'
includes:
  k:
    taskfile: kubectl.yml
    vars:
      n: awx
      k: awx
tasks:
  default:
    deps:
      - apply
  apply:
    cmds:
      - task: k:ns-create
      - task: k:ns-label
      - task: k:kapply
  delete:
    cmds:
      - task: k:kdelete
