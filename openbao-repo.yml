---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: openbao
      url: https://openbao.github.io/openbao-helm
tasks:
  default:
    deps:
      - task: add
