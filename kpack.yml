---
version: '3'
includes:
  kk:
    internal: true
    taskfile: kubectl.yml
    vars:
      n: kpack
      k: kpack
tasks:
  default:
    deps:
      - apply
  apply:
    cmds:
      - task: kk:kapply
  delete:
    cmds:
      - task: kk:kdelete
