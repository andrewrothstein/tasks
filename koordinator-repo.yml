---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    vars:
      n: koordinator-sh
      url: https://koordinator-sh.github.io/charts
tasks:
  default:
    deps:
      - task: helm-repo:add
