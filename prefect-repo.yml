---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: prefect
      url: https://prefecthq.github.io/prefect-helm
tasks:
  default:
    deps:
      - task: add
