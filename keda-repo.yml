---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: kedacore
      url: https://kedacore.github.io/charts
tasks:
  default:
    deps:
      - task: add
