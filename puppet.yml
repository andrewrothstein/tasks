---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: puppet
      url: https://puppetlabs.github.io/puppetserver-helm-chart
tasks:
  default:
    deps:
      - task: add
