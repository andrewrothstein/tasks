---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: stakater
      url: https://stakater.github.io/stakater-charts
tasks:
  default:
    deps:
      - task: add
