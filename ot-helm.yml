---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    internal: true
    vars:
      n: ot-helm
      url: https://ot-container-kit.github.io/helm-charts
tasks:
  default:
    deps:
      - task: helm-repo:add
