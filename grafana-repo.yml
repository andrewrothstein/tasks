---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: grafana
      url: https://grafana.github.io/helm-charts
tasks:
  default:
    deps:
      - task: add
