---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: percona
      url:  https://percona.github.io/percona-helm-charts/
tasks:
  default:
    deps:
      - task: add
