---
version: '3'
includes:
  repo: opencost-repo.yml
  kk:
    taskfile: kubectl.yml
    vars:
      n: opencost
      labels: 'istio.io/dataplane-mode=ambient'
  helm:
    taskfile: helm.yml
    vars:
      chart_repo_name: opencost
      chart: opencost
      chart_ver: 1.43.0
      namespace: opencost
      name: opencost
      helm_args: -f opencost-values.yml
  ingress:
    taskfile: helm.yml
    vars:
      chart: chart-tailscale-ingress
      namespace: opencost
      name: tailscale-ingress
      helm_args: -f opencost-tailscale-ingress-values.yml
tasks:
  default:
    deps:
      - upgrade
  upgrade:
    deps:
      - task: repo
    cmds:
      - task: kk:ns-create
      - task: kk:ns-label
      - task: helm:upgrade
      - task: ingress:upgrade
  delete:
    cmds:
      - task: ingress:delete
      - task: helm:delete
      - task: kk:ns-delete
  check:
    deps:
      - task: repo
    cmds:
      - task: helm:check
