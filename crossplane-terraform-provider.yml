---
version: '3'
includes:
  kk:
    flatten: true
    taskfile: kubectl.yml
    vars:
      n: crossplane-system
      k: crossplane-terraform-provider
tasks:
  default:
    deps:
      - task: kapply
