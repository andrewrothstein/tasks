---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: awx-operator
      url: https://ansible-community.github.io/awx-operator-helm/
tasks:
  default:
    deps:
      - task: add
