---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: clickhouse-operator
      url: https://docs.altinity.com/clickhouse-operator
tasks:
  default:
    deps:
      - task: add
