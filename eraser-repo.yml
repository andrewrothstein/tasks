---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: eraser
      url: https://eraser-dev.github.io/eraser/charts
tasks:
  default:
    deps:
      - task: add
