---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: weaviate
      url: https://weaviate.github.io/weaviate-helm
tasks:
  default:
    deps:
      - task: add
