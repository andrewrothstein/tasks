---
workflow:
  serviceAccount:
    create: true
    name: workflows
  rbac:
    create: true
controller:
  workflowNamespaces:
    - argo
    - vault-system
server:
  ingress:
    enabled: true
    ingressClassName: tailscale
    hosts:
      - argo
    tls:
      - hosts:
          - argo
