---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: community-charts
      url: https://community-charts.github.io/helm-charts
tasks:
  default:
    deps:
      - task: add
