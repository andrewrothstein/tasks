---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    vars:
      n: external-secrets
      url: https://charts.external-secrets.io
tasks:
  default:
    deps:
      - task: helm-repo:add
