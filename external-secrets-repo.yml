---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: external-secrets
      url: https://charts.external-secrets.io
tasks:
  default:
    deps:
      - task: add
