---
version: '3'
tasks:
  apply:
    vars:
      n: '{{ .n }}'
      s: '{{ .s }}'
      k: '{{ .k }}'
      v: '{{ .v }}'
    cmds:
      - |
        kubectl \
          create \
          secret \
          generic \
          -n {{ .n | quote }} \
          {{ .s | quote }} \
          "--from-literal={{ .k }}={{ .v }}"
    status:
      - kubectl get secret -n {{ .n | quote }} {{ .s | quote }}
  delete:
    vars:
      n: '{{ .n }}'
      s: '{{ .s }}'
    cmds:
      - |
        kubectl \
          delete \
          secret \
          -n {{ .n | quote }} \
          {{ .s | quote }}
