---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    internal: true
    vars:
      n: bitnami
      url: https://charts.bitnami.com/bitnami
tasks:
  default:
    deps:
      - task: helm-repo:add
