---
version: '3'
includes:
  repo: sealed-secrets-repo.yml
  helm:
    taskfile: helm.yml
    internal: true
    vars:
      NAMESPACE: kube-system
      NAME: sealed-secrets
      CHART_REPO_NAME: sealed-secrets
      CHART: sealed-secrets
      CHART_VER: 2.7.6
tasks:
  default:
    deps:
      - install
  install:
    deps:
      - task: repo
    cmds:
      - task: helm:install
  delete:
    cmds:
      - task: helm:delete
