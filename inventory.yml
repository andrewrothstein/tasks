---
all:
  hosts:
    localhost:
      ansible_connection: local
    truenas:
      ansible_user: root
    gateway:
      ansible_user: arothste
    console:
      ansible_user: arothste
  children:
    docker_zfs:
      hosts:
        console:
        gateway:
      vars:
        docker_engine_daemon_json:
          storage-driver: zfs
    docker:
      hosts:
        console:
        gateway:
