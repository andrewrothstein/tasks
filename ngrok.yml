---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    internal: true
    vars:
      n: ngrok
      url: https://ngrok.github.io/kubernetes-ingress-controller
tasks:
  default:
    deps:
      - task: helm-repo:add
