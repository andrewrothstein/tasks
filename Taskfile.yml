---
version: '3'
includes:
  k3d: k3d.yml
  kind: kind+cilium+registry.yml
  k8s: kind+cilium+registry.yml
  cni: cilium.yml
#  istio: istio.yml
  pre-mesh: pre-mesh.yml
  post-mesh: post-mesh.yml
tasks:
  up:
    cmds:
      - task: k8s:up
      - task: cni:install
#      - task: pre-mesh:up
#      - task: istio:up
#      - task: post-mesh:up
  delete:
    cmds:
#      - task: post-mesh:delete
#      - task: istio:delete
#      - task: cilium:delete
#      - task: pre-mesh:delete
      - task: cni:delete
      - task: k8s:delete
  default:
    deps:
      - up
