---
version: '3'
includes:
  ansible:
    internal: true
    taskfile: ansible.yml
    vars:
      i: 'localhost,'
      c: local
      target: localhost
      limit: localhost
tasks:
  ping:
    deps:
      - ansible:ping
  playbook:
    deps:
      - ansible:playbook
