---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    internal: true
    vars:
      n: strimzi
      url: https://strimzi.io/charts
tasks:
  default:
    deps:
      - task: helm-repo:add
