---
version: '3'
includes:
  repo: jetstack.yml
  kk:
    taskfile: kubectl.yml
    vars:
      n: cert-manager
      labels: 'istio.io/dataplane-mode=ambient'
      k: cert-manager
  ca-secret:
    taskfile: k8s-literal-secret.yml
    vars:
      n: cert-manager
      s: ca-key-pair
      args: >-
        --from-literal=tls.crt="${CA_CERT}"
        --from-literal=tls.key="${CA_KEY}"
  cm:
    taskfile: helm.yml
    vars:
      namespace: cert-manager
      name: cert-manager
      chart_repo_name: jetstack
      chart_ver: v1.16.3
      chart: cert-manager
      helm_args: -f cert-manager-values.yml
  approver_policy:
    taskfile: helm.yml
    vars:
      namespace: cert-manager
      name: cert-manager-approver-policy
      chart_repo_name: jetstack
      chart_ver: v0.19.0
      chart: cert-manager-approver-policy
      helm_args: -f cert-manager-approver-policy-values.yml
  csi_driver:
    taskfile: helm.yml
    vars:
      namespace: cert-manager
      name: cert-manager-csi-driver
      chart_repo_name: jetstack
      chart_ver: v0.10.2
      chart: cert-manager-csi-driver
  trust_manager:
    taskfile: helm.yml
    vars:
      namespace: cert-manager
      name: trust-manager
      chart_repo_name: jetstack
      chart_ver: v0.15.0
      chart: trust-manager
      helm_args: -f trust-manager-values.yml
tasks:
  default:
    deps:
      - upgrade
  check:
    deps:
      - task: repo
    cmds:
      - task: cm:check
      - task: approver_policy:check
      - task: csi_driver:check
      - task: trust_manager:check
  upgrade:
    deps:
      - task: repo
    cmds:
      - task: kk:ns-create
      - task: kk:ns-label
      - task: ca-secret:create
      - task: cm:upgrade
      - task: approver_policy:upgrade
      - task: csi_driver:upgrade
      - task: trust_manager:upgrade
      - task: kk:kapply
  delete:
    cmds:
      - task: kk:kdelete
      - task: trust_manager:delete
      - task: csi_driver:delete
      - task: approver_policy:delete
      - task: cm:delete
      - task: ca-secret:delete
      - task: kk:ns-delete
