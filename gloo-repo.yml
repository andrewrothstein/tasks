---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: gloo
      url: https://storage.googleapis.com/solo-public-helm
tasks:
  default:
    deps:
      - task: add
