---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: artifact-hub
      url: https://artifacthub.github.io/helm-charts
tasks:
  default:
    deps:
      - task: add
