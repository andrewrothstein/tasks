---
version: '3'
dotenv:
includes:
  cicd:
    taskfile: terraform-cicd.yml
    flatten: true
    vars:
      WORKSPACE_NAME: cloudflare-lmgtfy
      CHDIR: tf-cloudflare-lmgtfy
      tfvars: >-
        -var "api_token=${CLOUDFLARE_DNS_TOKEN}"
