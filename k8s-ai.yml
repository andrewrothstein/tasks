---
version: '3'
includes:
#  qdrant: qdrant.yml
  qdrant: pet.yml

#  k8sgpt: k8sgpt.yml
  k8sgpt: pet.yml

#  milvus: milvus.yml
  milvus: pet.yml

#  mlflow: mlflow.yml
  mlflow: pet.yml

  open-webui: open-webui.yml
#  open-webui: pet.yml

#  kubeai: kubeai.yml
  kubeai: pet.yml

#  weaviate: weaviate-helm.yml
  weaviate: pet.yml
tasks:
  default:
    deps:
      - up
  up:
    cmds:
      - task: qdrant:upgrade
      - task: k8sgpt:upgrade
      - task: milvus:upgrade
      - task: mlflow:upgrade
      - task: open-webui:upgrade
      - task: kubeai:upgrade
      - task: weaviate:upgrade
  delete:
    cmds:
      - task: weaviate:delete
      - task: kubeai:delete
      - task: open-webuid:delete
      - task: mlflow:delete
      - task: milvus:delete
      - task: k8sgpt:delete
      - task: qdrant:delete
  check:
    cmds:
      - task: qdrant:check
      - task: k8sgpt:check
      - task: milvus:check
      - task: mlflow:check
      - task: open-webui:check
      - task: kubeai:check
      - task: weaviate:check
