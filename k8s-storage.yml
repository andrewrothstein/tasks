---
version: '3'

includes:

#  longhorn: longhorn.yml
  longhorn: pet.yml

#  csi-driver-nfs: csi-driver-nfs.yml
  csi-driver-nfs: pet.yml

#  trident: trident.yml
  trident: pet.yml

#  minio-operator: minio-operator.yml
  minio-operator: pet.yml

#  velero: velero.yml
  velero: pet.yml

#  rook: rook.yml
  rook: pet.yml

tasks:
  default:
    deps:
      - up
  up:
    cmds:
      - task: rook:upgrade
      - task: longhorn:upgrade
      - task: csi-driver-nfs:upgrade
      - task: trident:upgrade
      - task: minio-operator:upgrade
      - task: velero:upgrade
  delete:
    cmds:
      - task: velero:delete
      - task: minio-operator:delete
      - task: trident:delete
      - task: csi-driver-nfs:delete
      - task: longhorn:delete
      - task: rook:delete
  check:
    cmds:
      - task: rook:check
      - task: longhorn:check
      - task: csi-driver-nfs:check
      - task: trident:check
      - task: minio-operator:check
      - task: velero:check
