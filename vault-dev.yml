---
version: '3'
includes:
  vault: vault.yml
tasks:
  default:
    deps:
      - install
  install:
    cmds:
      - task: vault:install
        vars:
          HELM_ARGS: -f vault-dev/values.yml
  delete:
    cmds:
      - task: vault:delete
