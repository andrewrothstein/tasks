---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: milvus
      url: https://milvus-io.github.io/milvus-helm
tasks:
  default:
    deps:
      - task: add
