---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    vars:
      n: milvus
      url: https://milvus-io.github.io/milvus-helm
tasks:
  default:
    deps:
      - task: helm-repo:add
