{{- $fullname := include "tailscale-ingress.fullname" . }}
{{- $labels := include "tailscale-ingress.labels" . }}
{{- $subdomain := .Values.subdomain }}
{{- range $i := .Values.ingresses }}
{{- $name := $i.name }}
{{- $host := include "tailscale-ingress.host" (dict "host" $i.host "subdomain" $subdomain) }}
{{- if (.enabled | default true) }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullname }}-{{ $name }}
  labels:
    {{- $labels | nindent 4 }}
  {{- with .annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .ingressClassName | default "tailscale" }}
  {{- if (.tls | default true) }}
  tls:
    - hosts:
        - {{ $host | quote }}
  {{- end }}
  rules:
    - host: {{ $host | quote }}
      http:
        paths:
          - path: {{ $i.path | default "/" }}
            pathType: {{ $i.pathType | default "ImplementationSpecific" }}
            backend:
              service:
                {{- toYaml $i.service | nindent 16 }}
{{- end }}
{{- end }}
