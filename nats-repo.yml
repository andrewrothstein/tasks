---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: nats
      url: https://nats-io.github.io/k8s/helm/charts
tasks:
  default:
    deps:
      - task: add
