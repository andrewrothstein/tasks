---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: fluxcd-community
      url: https://fluxcd-community.github.io/helm-charts
tasks:
  default:
    deps:
      - task: add
