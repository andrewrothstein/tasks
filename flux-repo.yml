---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    internal: true
    vars:
      n: fluxcd-community
      url: https://fluxcd-community.github.io/helm-charts
tasks:
  default:
    deps:
      - task: helm-repo:add
