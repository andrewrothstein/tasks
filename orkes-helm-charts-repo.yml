---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: orkes-helm-charts
      url: https://orkes-io.github.io/orkes-helm-charts
tasks:
  default:
    deps:
      - task: add
