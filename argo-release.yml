---
version: '3'
includes:
  repo: argo-repo.yml
  helm:
    taskfile: helm.yml
    vars:
      chart_repo_name: argo
      create_namespace: 'true'
      install: 'true'
tasks:
  upgrade:
    vars:
      chart: '{{ .chart }}'
      chart_ver: '{{ .chart_ver }}'
      name: '{{ .name }}'
      namespace: '{{ .namespace }}'
      helm_args: '{{ .helm_args }}'
    deps:
      - task: repo
    cmds:
      - task: helm:upgrade
        vars:
          chart: '{{ .chart }}'
          chart_ver: '{{ .chart_ver }}'
          name: '{{ .name }}'
          namespace: '{{ .namespace }}'
          helm_args: '{{ .helm_args }}'
  delete:
    vars:
      name: '{{ .name }}'
      namespace: '{{ .namespace }}'
    cmds:
      - task: helm:delete
        vars:
          name: '{{ .name }}'
          namespace: '{{ .namespace }}'
  check:
    vars:
      chart: '{{ .chart }}'
      chart_ver: '{{ .chart_ver }}'
    deps:
      - task: repo
    cmds:
      - task: helm:check
        vars:
          chart: '{{ .chart }}'
          chart_ver: '{{ .chart_ver }}'
