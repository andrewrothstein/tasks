---
version: '3'
includes:
  helm-repo:
    taskfile: helm-repo.yml
    internal: true
    vars:
      n: gatekeeper
      url: https://open-policy-agent.github.io/gatekeeper/charts
tasks:
  default:
    deps:
      - task: helm-repo:add
