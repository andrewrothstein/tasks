---
version: '3'
includes:
  repo:
    taskfile: helm-repo.yml
    flatten: true
    vars:
      n: gatekeeper
      url: https://open-policy-agent.github.io/gatekeeper/charts
tasks:
  default:
    deps:
      - task: add
